# 变更日志

本文档记录该项目的所有重要变更。

## [Unreleased]

### 新增
- 扫描引擎支持 overlay 优先调度：弹框打开时仅执行弹框内操作，页面操作标记为 `deferred` 延后
- 操作注册表新增 `deferred` 状态展示「延后(弹框优先)」

### 修复
- 弹框场景下不再误点页面背景元素；弹框内操作耗尽后自动关闭再继续页面扫描

## [1.0.0] - 2026-07-28

### 新增
- 初始化健康扫描工具分层架构：`core`、`server`、`web`。
- 新增 `core/enums` 枚举模块，统一领域常量与类型表达。
- 新增扫描会话生命周期与扫描配置项管理能力。
- 新增规则引擎 v3，支持四类规则驱动：
  - `text`（文本）
  - `attribute`（属性）
  - `selector`（元素选择器）
  - `parent`（元素父级链路）
- 新增激进点击探测流程：候选收集、定位解析、执行与结果归档。
- 新增网络问题采集器，用于识别请求失败和异常响应。
- 新增布局探针，用于识别布局与可见性异常。
- 新增规则配置持久化能力，规则写入 `Storage/config` 并支持默认回退。
- 新增规则配置相关接口：
  - `GET /api/rules`
  - `POST /api/rules`
  - `POST /api/rules/reset`
  - `POST /api/rules/open-file`
- 新增规则配置抽屉交互能力：
  - 顶部操作区固定、规则列表滚动
  - 规则基础信息弹窗新增/编辑
  - 配置目录提示与一键打开配置文件夹
- 新增规则值标签编辑能力：支持新增、删除、点击编辑及 hover 交互样式。
